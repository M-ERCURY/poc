This folder contains user-defined mercury scripts and may include
locally-modified versions of default scripts.

If identically named scripts exist both here and in the "default"
folder, this folder is given precedence when `mercury exec` is ran.
